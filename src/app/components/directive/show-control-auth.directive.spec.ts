import { ShowControlAuthDirective } from './show-control-auth.directive';

describe('ShowControlAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowControlAuthDirective();
    expect(directive).toBeTruthy();
  });
});
